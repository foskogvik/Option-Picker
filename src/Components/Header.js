import { StyledHeader, Title, Subtitle } from "./styles/Header.styled";

const Header = () => {
  return (
    <StyledHeader>
      <Title>Option picker</Title>
      <Subtitle>Throw in some options and see what you get</Subtitle>
    </StyledHeader>
  );
};

export default Header;
